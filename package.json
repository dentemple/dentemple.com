{
  "name": "dentemple.com",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "license": "MIT",
  "scripts": {
    "start": "npm run start:profile",
    "start:profile": "cd profile-docs && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
